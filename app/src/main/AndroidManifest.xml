<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thekoladeakande.cryptoconverter">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity
            android:name=".menuActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BtcActivity"
            android:theme="@style/BtcTheme" />
        <activity
            android:name=".EthActivity"
            android:theme="@style/EthTheme">

        </activity>
        <activity
            android:name=".CurrencyActivity"
            android:theme="@style/CurrencyTheme">

        </activity>
    </application>

</manifest>